# ApplicationSets

Argo CD `ApplicationSet` resources for this cluster live here.

Use this folder when applications should be generated from directories or files,
for example one Argo CD Application per directory under `apps/` or
`infrastructure/`.
