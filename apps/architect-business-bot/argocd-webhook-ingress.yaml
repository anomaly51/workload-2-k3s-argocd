apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: architect-business-bot-argocd-webhook
  namespace: argocd
  annotations:
    external-dns.alpha.kubernetes.io/hostname: argocd-webhook.api-api-api.com
    external-dns.alpha.kubernetes.io/target: a6aaa50f-2590-4179-84f2-d0f18bce0587.cfargotunnel.com
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    external-dns.alpha.kubernetes.io/ttl: "120"
    traefik.ingress.kubernetes.io/service.serversscheme: https
spec:
  ingressClassName: traefik
  rules:
    - host: argocd-webhook.api-api-api.com
      http:
        paths:
          - path: /api/webhook
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  name: https
